---
const industries = [
  { name: 'Agriculture', icon: '🌿', desc: 'Honey blending, farm management, supply chain' },
  { name: 'Construction', icon: '🏗️', desc: 'Building audits, safety compliance, project management' },
  { name: 'Hospitality', icon: '🏨', desc: 'Booking systems, CRM, property management' },
  { name: 'Infrastructure', icon: '⚡', desc: 'Drone inspection, asset monitoring, compliance' },
  { name: 'Manufacturing', icon: '🏭', desc: 'Process automation, inventory, quality control' },
  { name: 'Professional Services', icon: '📊', desc: 'Document signing, time tracking, client portals' },
];
---

<section class="section-padding relative overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-kiwi-500/[0.015] to-transparent"></div>

  <div class="container-wide relative">
    <!-- Quote block -->
    <div class="max-w-4xl mx-auto text-center mb-24" data-animate="scale">
      <div class="relative">
        <div class="absolute -top-6 left-1/2 -translate-x-1/2 text-kiwi-500/10 text-[120px] font-serif leading-none select-none">"</div>
        <blockquote class="relative font-display text-2xl md:text-4xl text-white leading-snug mb-8 pt-8 text-balance">
          Every business is a digital business.<br />
          <span class="gradient-text font-bold">Some just don't know it yet.</span>
        </blockquote>
        <div class="glow-line w-16 mx-auto mb-8"></div>
        <p class="text-gray-400 text-lg leading-relaxed max-w-xl mx-auto">
          We believe the next generation of NZ businesses won't just <em>use</em> AI — they'll be
          <em>built</em> on it. Our job is to make that transition effortless.
        </p>
      </div>
    </div>

    <!-- Industries grid -->
    <div data-animate>
      <p class="text-center text-xs font-mono text-gray-600 uppercase tracking-[0.2em] mb-10">
        Industries We Transform
      </p>
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3">
        {industries.map((ind, i) => (
          <div class="glass rounded-xl p-4 text-center hover-lift spotlight group cursor-default" style={`transition-delay: ${i * 60}ms;`}>
            <div class="text-2xl mb-2">{ind.icon}</div>
            <div class="text-sm font-medium text-white mb-1">{ind.name}</div>
            <div class="text-[10px] text-gray-500 leading-tight">{ind.desc}</div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
